<!DOCTYPE html><html lang="pt-BR"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Documentos de empresas - {{ config('app.name','Stagium') }}</title>@fonts
<style>
:root{--bg:#f6f8fb;--surface:#fff;--text:#122033;--muted:#637083;--border:#d8e2ee;--accent:#155ea8;--soft:#eef4fb;--danger:#b42318;--success:#18794e;--success-bg:#e7f5ee;--success-border:#b8dec9;font-family:"Instrument Sans",ui-sans-serif,system-ui,sans-serif}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--text)}.page{min-height:100vh;padding:40px 24px}.wrap{width:min(100%,1100px);margin:auto}.header{display:flex;align-items:flex-end;justify-content:space-between;gap:16px;border-bottom:1px solid var(--border);padding-bottom:24px}.brand{margin:0 0 4px;color:var(--accent);font-size:14px;font-weight:700}h1{margin:0;font-size:clamp(28px,4vw,36px)}h2{margin:0 0 4px;font-size:20px}p{margin-top:0}.tabs{display:flex;gap:4px;border:1px solid var(--border);border-radius:8px;background:#fff;padding:4px}.tab{border-radius:6px;color:var(--muted);padding:8px 14px;font-size:14px;font-weight:700;text-decoration:none;white-space:nowrap}.tab.active{background:var(--accent);color:#fff}.module-tabs{display:flex;gap:24px;margin-top:24px;border-bottom:1px solid var(--border)}.module-tab{border-bottom:3px solid transparent;color:var(--muted);padding:0 2px 12px;font-size:14px;font-weight:750;text-decoration:none}.module-tab.active{border-color:var(--accent);color:var(--accent)}.status{margin-top:24px;border:1px solid var(--success-border);border-radius:8px;background:var(--success-bg);color:#145235;padding:12px 16px;font-size:14px}.panel{margin-top:24px;border:1px solid var(--border);border-radius:8px;background:var(--surface);padding:24px;box-shadow:0 8px 24px rgba(18,32,51,.06)}.company-select{display:grid;grid-template-columns:1fr auto;gap:12px;align-items:end}.field label{display:block;margin-bottom:8px;font-size:14px;font-weight:700}select,input[type=file]{width:100%;border:1px solid #c8d5e4;border-radius:6px;background:#fff;padding:10px 12px;font:inherit;font-size:14px}.button{display:inline-block;border:0;border-radius:6px;background:var(--accent);color:#fff;padding:11px 15px;font:inherit;font-size:14px;font-weight:700;text-decoration:none;cursor:pointer}.summary{display:flex;align-items:center;justify-content:space-between;gap:16px;margin-top:28px}.progress{color:var(--muted);font-size:14px}.group-title{margin:28px 0 10px;color:var(--accent);font-size:15px}.document-list{border:1px solid var(--border);border-radius:8px;overflow:hidden}.document-row{display:grid;grid-template-columns:minmax(260px,1fr) minmax(280px,1fr);gap:20px;align-items:center;padding:16px;border-bottom:1px solid var(--border)}.document-row:last-child{border-bottom:0}.doc-name{margin:0 0 5px;font-size:14px;font-weight:700}.badge{display:inline-block;border-radius:999px;background:#fff1f0;color:var(--danger);padding:3px 8px;font-size:12px;font-weight:700}.badge.done{background:var(--success-bg);color:var(--success)}.file-meta{margin:5px 0 0;color:var(--muted);font-size:12px}.upload-form{display:grid;grid-template-columns:1fr auto;gap:8px}.file-actions{display:flex;align-items:center;justify-content:flex-end;gap:6px}.link,.delete{border:0;background:transparent;border-radius:6px;padding:8px 10px;font:inherit;font-size:13px;font-weight:700;text-decoration:none;cursor:pointer}.link{color:var(--accent)}.delete{color:var(--danger)}.empty{text-align:center;padding:38px;color:var(--muted)}.error{margin:8px 0 0;color:var(--danger);font-size:13px}@media(max-width:800px){.header{align-items:flex-start;flex-direction:column}.tabs{width:100%;overflow:auto}.tab{flex:1;text-align:center}.document-row{grid-template-columns:1fr}.file-actions{justify-content:flex-start}}@media(max-width:560px){.page{padding:24px 14px}.panel{padding:17px}.company-select,.upload-form{grid-template-columns:1fr}.module-tabs{gap:14px}.module-tab{font-size:13px}}
</style><style>.search-row{display:grid;grid-template-columns:1fr auto auto;gap:10px;align-items:end;margin-bottom:20px}.clear-button{display:inline-block;border:1px solid var(--border);border-radius:6px;background:#fff;color:var(--muted);padding:10px 14px;font-size:14px;font-weight:700;text-decoration:none}.search-row input{width:100%;border:1px solid #c8d5e4;border-radius:6px;background:#fff;padding:10px 12px;font:inherit;font-size:14px}.term-box{margin:26px 0;border:1px solid var(--border);border-radius:8px;background:var(--soft);padding:20px}.student-picker{max-height:230px;overflow:auto;margin:14px 0;border:1px solid var(--border);border-radius:6px;background:#fff}.student-option{display:flex;gap:10px;align-items:flex-start;padding:11px 12px;border-bottom:1px solid var(--border);font-size:14px}.student-option:last-child{border-bottom:0}.student-option input{margin-top:3px}.term-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px}.term-grid input{width:100%;border:1px solid #c8d5e4;border-radius:6px;padding:10px 12px;font:inherit}.term-actions{display:flex;gap:8px;margin-top:10px}.term-row{display:flex;justify-content:space-between;gap:12px;align-items:center;padding:12px 0;border-top:1px solid var(--border)}@media(max-width:560px){.search-row,.term-grid{grid-template-columns:1fr}.search-row .field{grid-column:1/-1}}</style></head><body><div class="page"><main class="wrap">
<style>.generator{margin:24px 0;border:1px solid var(--border);border-radius:8px;background:var(--soft);padding:20px}.generator-grid{display:grid;grid-template-columns:1fr 1fr 1fr;gap:13px}.generator-grid input{width:100%;border:1px solid #c8d5e4;border-radius:6px;background:#fff;padding:10px 12px;font:inherit}.generator .wide{grid-column:1/-1}.generator .button{margin-top:16px}.generator-note{color:var(--muted);font-size:14px}@media(max-width:760px){.generator-grid{grid-template-columns:1fr}.generator .wide{grid-column:auto}}</style>
<header class="header"><div><p class="brand">Stagium</p><h1>Documentos de empresas</h1></div><nav class="tabs"><a class="tab" href="{{ route('courses.index') }}">Início</a><a class="tab" href="{{ route('students.index') }}">Alunos</a><a class="tab active" href="{{ route('companies.index') }}">Empresas</a><a class="tab" href="{{ route('profile.edit') }}">Meus dados</a></nav></header>
<nav class="module-tabs"><a class="module-tab" href="{{ route('companies.index') }}">Cadastrar empresa</a><a class="module-tab active" href="{{ route('companies.documents.index') }}">Documentos de empresas</a><a class="module-tab" href="{{ route('companies.terms.index') }}">Termos de encaminhamento</a></nav>
@if(session('status'))<div class="status">{{ session('status') }}</div>@endif
<section class="panel">
<form class="search-row" action="{{ route('companies.documents.index') }}" method="GET"><div class="field"><label for="q">Buscar empresa</label><input id="q" name="q" type="search" value="{{ $search }}" placeholder="Nome fantasia, razão social ou CNPJ"></div><button class="button" type="submit">Buscar</button>@if($search !== '')<a class="clear-button" href="{{ route('companies.documents.index') }}">Limpar</a>@endif</form>
@if($companies->isEmpty())
    @if($hasCompanies)<div class="empty"><h2>Nenhuma empresa encontrada</h2><p>Tente buscar por outro nome ou pelos números do CNPJ.</p></div>@else<div class="empty"><h2>Nenhuma empresa cadastrada</h2><p>Cadastre uma empresa antes de anexar documentos.</p><a class="button" href="{{ route('companies.index') }}">Cadastrar empresa</a></div>@endif
@else
    <form class="company-select" action="{{ route('companies.documents.index') }}" method="GET"><input type="hidden" name="q" value="{{ $search }}"><div class="field"><label for="company_id">Empresa</label><select id="company_id" name="company_id">@foreach($companies as $company)<option value="{{ $company->id }}" @selected($selectedCompany?->id === $company->id)>{{ $company->trade_name }} — {{ $company->corporate_name }}</option>@endforeach</select></div><button class="button" type="submit">Carregar documentos</button></form>
    @php($attached = $selectedCompany->documents->keyBy('type'))
    @php($mergeableCount = $attached->except('minuta_termo')->count())
    <div class="summary"><div><h2>{{ $selectedCompany->trade_name }}</h2><p class="progress">{{ $attached->count() }} de {{ count($documentTypes) }} documentos anexados</p></div>@if($mergeableCount > 0)<a class="button" href="{{ route('companies.documents.download-all', $selectedCompany) }}">Baixar PDF único</a>@endif</div>
    <section class="generator"><h2>Gerar documentos oficiais</h2><p class="generator-note">Preencha os dados complementares para gerar automaticamente a Minuta de Convênio, o Formulário de Celebração e a C.I. de Solicitação.</p>@error('generation')<p class="error">{{ $message }}</p>@enderror<form action="{{ route('companies.documents.generate-official',$selectedCompany) }}" method="POST">@csrf<div class="generator-grid">
    <div class="field"><label>Nº do convênio</label><input name="agreement_number" value="{{ old('agreement_number') }}" placeholder="Ex.: 015" required></div><div class="field"><label>Nº da C.I.</label><input name="ci_number" value="{{ old('ci_number') }}" placeholder="Ex.: 025" required></div><div class="field"><label>Data dos documentos</label><input name="document_date" type="date" value="{{ old('document_date',now()->format('Y-m-d')) }}" required></div>
    <div class="field"><label>IEMA Pleno</label><input name="iema_unit" value="{{ old('iema_unit') }}" required></div><div class="field"><label>Código da unidade/IP</label><input name="iema_code" value="{{ old('iema_code') }}" placeholder="Ex.: IP-SÃO LUÍS" required></div><div class="field"><label>Gestor(a) Geral</label><input name="manager_name" value="{{ old('manager_name') }}" required></div>
    <div class="field"><label>Quantidade de vagas</label><input name="vacancies" type="number" min="1" max="999" value="{{ old('vacancies',1) }}" required></div><div class="field"><label>Órgão expedidor do RG</label><input name="issuing_authority" value="{{ old('issuing_authority') }}" placeholder="Ex.: SSP/MA" required></div><div class="field"><label>Área de atuação</label><input name="business_area" value="{{ old('business_area') }}" required></div>
    <div class="field"><label>Cidade da empresa</label><input name="company_city" value="{{ old('company_city') }}" required></div><div class="field"><label>UF da empresa</label><input name="company_state" maxlength="2" value="{{ old('company_state','MA') }}" required></div><div class="field"><label>CEP da empresa</label><input name="company_zip" value="{{ old('company_zip') }}" required></div><div class="field wide"><label>E-mail da empresa</label><input name="company_email" type="email" value="{{ old('company_email') }}" required></div>
    <div class="field wide"><label>Endereço para envio do convênio</label><input name="shipping_address" value="{{ old('shipping_address',$selectedCompany->formattedAddress()) }}" required></div><div class="field"><label>Cidade para envio</label><input name="shipping_city" value="{{ old('shipping_city') }}" required></div><div class="field"><label>UF para envio</label><input name="shipping_state" maxlength="2" value="{{ old('shipping_state','MA') }}" required></div><div class="field"><label>CEP para envio</label><input name="shipping_zip" value="{{ old('shipping_zip',$selectedCompany->address_zip) }}" required></div>
    <div class="field"><label>Responsável pela entrega</label><input name="delivery_responsible" value="{{ old('delivery_responsible',$selectedCompany->responsible_name) }}" required></div><div class="field"><label>Telefone para entrega</label><input name="delivery_phone" value="{{ old('delivery_phone',$selectedCompany->responsible_phone) }}" required></div><div class="field"><label>E-mail para entrega</label><input name="delivery_email" type="email" value="{{ old('delivery_email') }}" required></div></div><button class="button" type="submit">Gerar os três documentos em PDF</button></form></section>
    @foreach(collect($documentTypes)->groupBy('group', true) as $group => $types)
        <h3 class="group-title">{{ $group }}</h3><div class="document-list">
        @foreach($types as $type => $definition)
            @php($document = $attached->get($type))
            <div class="document-row"><div><p class="doc-name">{{ $definition['label'] }}</p>@if($document)<span class="badge done">Anexado</span><p class="file-meta">{{ $document->original_name }} · {{ number_format($document->size / 1024, 0, ',', '.') }} KB</p>@else<span class="badge">Pendente</span>@endif</div><div>
                @if($document)<div class="file-actions"><a class="link" href="{{ route('companies.documents.download',$document) }}">Baixar</a><form action="{{ route('companies.documents.destroy',$document) }}" method="POST" onsubmit="return confirm('Remover este documento?')">@csrf @method('DELETE')<button class="delete" type="submit">Remover</button></form></div>
                @else
                    <form class="upload-form" action="{{ route('companies.documents.store',[$selectedCompany,$type]) }}" method="POST" enctype="multipart/form-data">@csrf<input type="file" name="document" accept="application/pdf,.pdf" required><button class="button" type="submit">Anexar PDF</button></form>
                    @error('document')<p class="error">{{ $message }}</p>@enderror
                @endif
            </div></div>
        @endforeach</div>
    @endforeach
@endif
</section></main></div></body></html>
